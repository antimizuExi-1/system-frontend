# Vue 3 + Vite

这是一个基于 Vue 3 + Vite 构建的前端项目。

## 🚀 启动项目

### 1️⃣ 安装依赖

```bash
npm install
```

### 2️⃣ 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`：

```env
VITE_API_BASE_URL=http://localhost:3000
```

### 3️⃣ 启动服务

```bash
npm run dev
```